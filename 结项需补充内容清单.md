# e 分支功能合入与待补清单

当前 `e` 分支以最新 `main` 为基础，**保留 main 原来的首页、ECharts PCA 可视化和前端交互**。没有合入 `origin/C` 的整套页面体系，只把可复用的后端功能/API 补到 main 的结构里。

## 已合入的功能

| 功能 | 当前状态 | 说明 |
| --- | --- | --- |
| 用户认证 API | 已合入 | 新增 `/api/auth/register`、`/api/auth/login`、`/api/auth/me`、`/api/auth/users`、`/api/auth/users/<username>/role`。 |
| 条件检索 | 已合入并验证 | main 原 `/api/search` 增加 `filter_cell_type`，支持按细胞类型筛选 Top-K。 |
| Top-K 数量修复 | 已修复并验证 | 搜索时会多取候选，剔除查询细胞自身和筛选条件后仍尽量返回足够 Top-K。 |
| 索引状态与重建 API | 已合入并验证 | 新增 `/api/index/status`、`/api/index/rebuild`。 |
| 性能评估 API | 已合入并验证 | 新增 `/api/benchmark`，返回平均耗时、P50、P99、QPS 等指标。 |
| 查询向量 API | 已合入并验证 | 新增 `/api/search/by-vector`，并补充向量维度校验。 |

## 保留 main 的内容

- 保留 main 的 `templates/index.html`。
- 保留 main 的 `static/js/app.js`。
- 保留 main 的 `static/css/style.css`。
- 保留 main 的 `apps/home/routes.py`。
- 保留 main 的启动方式和 `apps.create_app` 结构。

## 没有合入的 origin/C 页面

以下页面没有合入，合入后前端页面改动较大：

- `dashboard.html`
- `data_import.html`
- `index_management.html`
- `login.html`
- `management.html`
- `user_info.html`
- `static/style.css`

## 已验证

- Flask 应用可以正常创建。
- main 首页 `/` 可正常返回，仍使用 ECharts PCA 页面。
- 默认管理员登录接口可用。
- `/api/status` 可返回当前数据状态。
- `/api/index/status` 可返回索引状态。
- `/api/benchmark` 可返回性能评估结果。
- 普通 Top-K 检索可返回足够数量结果。
- 条件检索可返回满足细胞类型筛选的结果。
- 完整 30 维向量查询可返回结果；2 维向量会返回明确维度错误。

## 仍需要补全

| 待补内容 | 原因 | 建议负责模块 |
| --- | --- | --- |
| 用户登录/用户信息前端入口 | 目前只合入了用户相关 API，没有合入 C 的登录页和用户页。 | 前端模块 + 后端集成模块 |
| 后端登录态和权限控制 | 当前认证 API 可用，但管理员接口还缺少严格后端鉴权。 | 后端集成模块 |
| 条件检索前端控件 | 后端已支持 `filter_cell_type`，main 页面还没有筛选框。 | 前端模块 |
| 查询向量前端入口 | 后端接口已可用，但页面上还没有输入完整向量的入口。 | 前端模块 + 检索核心模块 |
| 数据集管理与主搜索联动 | main 原数据集接口仍未完全影响主搜索使用的数据源。 | 后端集成模块 + 数据处理模块 |
| 更完整的索引管理页面 | 目前只补了状态/重建 API，没有合入 C 的索引管理页面。 | 前端模块 + 检索核心模块 |

